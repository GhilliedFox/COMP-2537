const profile = () => {
  return (
    <main>
      <h1>asdf</h1>
    </main>
  );
};

export default profile;

// export async function getPokemon(context) {
//   return {
//     async fetchPokemon() {
//       const response = await fetch(
//         `https://pokeapi.co/api/v2/pokemon/${context.query.name}`
//       );
//       const pokemon = await response.json();
//       return pokemon;
//     },
//   };
// }
